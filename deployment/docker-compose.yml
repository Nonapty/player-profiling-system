services:
  api:
    build:
      context: ..
      dockerfile: backend/player-profiling-api/Dockerfile
    ports:
      - "8000:8000"
    environment:
      - APP_NAME=Player Profiling System
      - DATA_MODE=real-first
